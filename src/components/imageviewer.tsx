"use client";
import Link from "next/link";
import { usePathname } from 'next/navigation';
import React from "react";

export default function ImageViewer(props: { imageSrc: string}) {
    const [open, setOpen] = React.useState(false);
    return (
        <></>
    );
}